diff a/src/components/Sidebar/Sidebar.js b/src/components/Sidebar/Sidebar.js	(rejected hunks)
@@ -137,10 +159,10 @@ function Sidebar() {
       <ExpandableNav theme={theme}>
         <NavButtons>
           {buttons.map(button => (
-            <NavWrapper theme={theme} key={button.id}>
+            <NavLinkStyled theme={theme} key={button.id} to={button.link}>
               <Icon name={button.icon} size={ICON_SIZE} />
               <div className="nav-link">{button.text}</div>
-            </NavWrapper>
+            </NavLinkStyled>
           ))}
         </NavButtons>
 
