/* Pseudocode for now
import charactercontext
import battlecontext

export function nextTurn({ characters, characterDispatch, battleDispatch }) {
  // const char = get which character should go first
  //
  // charDispatch({ type: 'next-turn', startingWith: char });
  //  o Loops
  // battleDispatch({ type: 'next-turn', startingWith: char });
}
*/
